apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hello-app-ingress
spec:
  rules:
    - host: ramazankaragoz.com
      http:
        paths:
          - path: /demo-docker
            backend:
              serviceName: hello-app-service
              servicePort: 80

